"""Storage layer: models, sessions, migrations.

Deliberately empty of code. Importing ``itgraph.db.session`` validates
the environment, so a package-level re-export would make even
``itgraph --help`` require a configured ``DATABASE_URL``.
"""
